#pragma once

#include "API.h"
#include "FinderSpec.h"

namespace FinderSpecs
{
  class FINDER_API Tie
  {
  public:
    const static FileSpec BUILT_FILE;
    const static FileSpec HELPER_FILE;
    const static FileSpec HELPER_ENUM_MANIFEST_FILE;
    const static FileSpec MENTALRAY_FILE;
  };
}
