#pragma once

#include "API.h"
#include "FinderSpec.h"

namespace FinderSpecs
{
  class FINDER_API Ufrag
  {
  public:
    const static FileSpec BUILT_FILE;
    const static FileSpec BUILT_ENUM_MANIFEST_FILE;
    const static FileSpec MENTALRAY_FILE;
  };
}
