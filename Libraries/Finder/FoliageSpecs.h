#pragma once

#include "API.h"
#include "FinderSpec.h"

namespace FinderSpecs
{
  class FINDER_API Foliage
  {
  public:
    const static FileSpec BUILT_FILE;
    const static FileSpec SPEEDTREE_FILE;
    const static FileSpec SPEEDTREE_TEXTURE_FILE;
  };
}
